/**
 * 设备性能评估（占位）。
 * TODO(M1/M3): 通过 WEBGL_debug_renderer_info 读取 GPU 信息，
 *              结合 deviceMemory / hardwareConcurrency 判定低端设备。
 */
export function isLowEndDevice(): boolean {
  return false;
}
