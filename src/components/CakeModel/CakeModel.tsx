/**
 * 蛋糕 3D 模型组件（占位）。
 * TODO(M2): useLoader 加载 GLB（< 600KB）、相对卡片缩放 0.4、上下浮动（不旋转）、
 *           卸载时 gltf.scene.traverse 释放 geometry/material。
 * 注意：@react-three/fiber / three 依赖将在 M1 Spike 确定版本后引入。
 */
export default function CakeModel() {
  return null;
}
